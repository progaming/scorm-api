{
  "name": "scorm-api",
  "version": "1.0.6",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "build": "babel src -d build --copy-files",
    "compile": "babel src -d .compiled --source-maps --watch",
    "test": "babel-node src/test.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^0.18.0",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
